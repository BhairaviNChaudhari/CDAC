
//1. Working with java.lang.Boolean
public class Day3 {

public static void main(string args[]) {
	
}
		 /*b.
		 * Declare a method-local variable status of type boolean with the value true
		 * and convert it to a String using the toString method. (Hint: Use
		 * Boolean.toString(Boolean) ).
		
		Boolean status = true;
		String str = Boolean.toString(status);
		System.out.println("Type of the string is :" +str);
	
        op :  Type of the string is :true 
		
		c. /*Declare a method-local variable strStatus of type String with the value "true" and 
		convert it to a boolean using the parseBoolean method. (Hint: Use Boolean.parseBoolean(String)).
		
		String strStatus = "true";
		Boolean b = Boolean.parseBoolean(strStatus);
		System.out.println("Type of the string is :" +b);
		op : Type of the string is :true  
		
		d. Declare a method-local variable strStatus of type String with the value "1" or "0" 
		and attempt to convert it to a boolean. (Hint: parseBoolean method will not work as expected with 
				"1" or "0").
		String strStatus= "1";
		Boolean b = Boolean.valueOf(strStatus);
		System.out.println("Type of the boolean is :" +b);
		op : Type of the boolean is :false
		
		e. Declare a method-local variable status of type boolean with the value 
		true and convert it to the corresponding wrapper
		class using Boolean.valueOf(). (Hint: Use Boolean.valueOf(boolean)).
		
		Boolean status = true ;
		Boolean a = Boolean.valueOf(status);
		System.out.println("Type of the boolean is :" +a);
		
		op : Type of the boolean is :true
		
		*f. Declare a method-local variable strStatus of type String with the value "true" 
		*and convert it to the corresponding wrapper 
		*class using Boolean.valueOf(). (Hint: Use Boolean.valueOf(String)).
		
		String strStatus = "true";
		Boolean b = Boolean.valueOf(strStatus);
		System.out.println("Type of the boolean is :" +b);
		
		op : Type of the boolean is :true
		
		g. Experiment with converting a boolean
		 value into other primitive types or vice versa and observe the results.
		
		Boolean b = false;
		String a = String.valueOf(b);
		int b = int.parseInt(b);
		System.out.println(""+b);
		*/
	
	
	
	
	
	
	}
}